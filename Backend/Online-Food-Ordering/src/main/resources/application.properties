spring.application.name=Online-Food-Ordering
server.port = 5454
spring.datasource.url = jdbc:mysql://${MYSQL_HOST:127.0.0.1}:3306/food_ordering_app
spring.datasource.username = root
spring.datasource.password = tonyherve_mysql
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver

# Optional: Enable logging of SQL queries for debugging
spring.jpa.show-sql = true

# Set the ddl-auto property to manage the database schema
# Options: none, validate, update, create, create-drop
spring.jpa.hibernate.ddl-auto = create-drop
